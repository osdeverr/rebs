type: executable
name: .tool

artifact-name: re

deps:
  - .buildkit
  - .resources

actions:
  #- install:
  #    on: deploy
  #    to:
  #      - ${re-deploy-path}

  - install:
      on: deploy
      to:
        - ${re-dev-deploy-path}

platform.windows:
  actions:
    - install:
        on: deploy
        from: ${build-artifact}
        to-file:
          - ${re-dev-deploy-path}/re-dev.exe

