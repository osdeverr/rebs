type: executable
name: .resources

exclude:
  - data
  - root

deps:
  - .buildkit
  - .tool
  - ninja
  # - ninja

actions:
  - copy:
      from: data
      to: data
  - copy:
      from: root
      to: .
