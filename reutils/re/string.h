#pragma once
#include <string>
#include <string_view>

namespace re
{
	// We will likely transition to some other string type in the future!
	using String = std::string;
	using StringView = std::string_view;
}
